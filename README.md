# Simple script that uses sharp to generate optimised images👋


## Get started

1. Install dependencies

   ```bash
   npm install
   bun install
   ```

2. Start the script (# check the package.json ) 

